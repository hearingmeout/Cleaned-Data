[{"id":"DBZ000003","type":"mcq","stimulusHtml":"<p class=\"dap_body\">Which of the following best describes the behavior of the code segment?</p>","features":[{"type":"sharedpassage","html":"<div class=\"stimulus_reference\"><p class=\"dap_body\">The following code segment is intended to round  <code class=\"dap_inline\">val</code>  to the nearest integer and print the result.</p><p class=\"dap_body\"> </p><pre><code class=\"language-plaintext\">double val = -0.7; \nint roundedVal = (int) (val + 0.5); \nSystem.out.println(roundedVal); </code></pre></div>"}],"choicesHtml":["<p class=\"dap_body\">The code segment works as intended.</p>","<p class=\"dap_body\">The code segment does not work as intended because the expression  <code class=\"dap_inline\">(val + 0.5)</code>  should be cast to a  <code class=\"dap_inline\">double</code>  instead of an  <code class=\"dap_inline\">int</code>.</p>","<p class=\"dap_body\">The code segment does not work as intended because  <code class=\"dap_inline\">val</code>  should be cast to an  <code class=\"dap_inline\">int</code>  before  <code class=\"dap_inline\">0.5</code>  is added to it.</p>","<p class=\"dap_body\">The code segment does not work as intended because the expression  <code class=\"dap_inline\">(int) (val + 0.5)</code>  rounds to the nearest integer only when  <code class=\"dap_inline\">val</code>  is positive.</p>"],"answer":"01JSDHJHD32H8HDC1X0EZC3F81"}]