[{"id":"HMI000005","type":"mcq","stimulusHtml":"<p class=\"dap_body\">Which of the following can be used to replace  <span class=\"dap_inline\">/* missing code */</span>  so that the code segment works as intended?</p>","features":[{"type":"sharedpassage","html":"<div class=\"stimulus_reference\"><p class=\"dap_body\">In the following code segment,  <span class=\"dap_inline\">str</span>  is a properly declared and initialized string. The code segment is intended to count the number of times that  <span class=\"dap_inline\">\"a\"</span>  appears in  <span class=\"dap_inline\">str</span>.</p><p class=\"dap_body\"> </p><pre><code class=\"language-plaintext\">int count = 0;\nString temp = str;\nint loc = temp.indexOf(\"a\");\n\nwhile (loc &gt;= 0)\n{\n   /* missing code */\n}</code></pre></div>"}],"choicesHtml":["<pre><code class=\"language-plaintext\">count++;\ntemp = str.substring(loc + 1);</code></pre>","<pre><code class=\"language-plaintext\">count++;\nloc++;\ntemp = str.substring(loc);</code></pre>","<pre><code class=\"language-plaintext\">count++;\ntemp = temp.substring(loc);\nloc = temp.indexOf(\"a\");</code></pre>","<pre><code class=\"language-plaintext\">count++;\ntemp = temp.substring(loc + 1);\nloc = temp.indexOf(\"a\");</code></pre>"],"answer":"01JEPQ4E350C2SRAWTR395J860"}]