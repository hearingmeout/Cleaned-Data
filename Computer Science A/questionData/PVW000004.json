[{"id":"PVW000004","type":"mcq","stimulusHtml":"<p class=\"dap_body\">Which of the following can be used to replace line 2 so that this code segment will compile without error?</p>","features":[{"type":"sharedpassage","html":"<div class=\"stimulus_reference\"><p class=\"dap_body\">The  <span class=\"dap_inline\">Dog</span>  class contains a non-<span class=\"dap_inline\">static</span>  method named  <span class=\"dap_inline\">calculateWalkLength</span>  which can be called from a class other than  <span class=\"dap_inline\">Dog</span>.  The method takes an integer parameter for the number of minutes a dog is taken for a walk and returns a  <span class=\"dap_inline\">double</span>  representing the number of miles walked during that time.</p><p class=\"dap_body\"> </p><p class=\"dap_body\">The following code segment appears in a class other than  <span class=\"dap_inline\">Dog</span>.  The statement in line 2 causes a compile-time error.</p><pre><code class=\"language-plaintext\">Dog fido = new Dog();                         // line 1\ndouble miles = calculateWalkLength(fido, 90); // line 2</code></pre></div>"}],"choicesHtml":["<pre><code class=\"language-plaintext\">double miles = calculateWalkLength(90);</code></pre>","<pre><code class=\"language-plaintext\">double miles = Dog.calculateWalkLength(fido, 90);</code></pre>","<pre><code class=\"language-plaintext\">double miles = Dog.calculateWalkLength(90);</code></pre>","<pre><code class=\"language-plaintext\">double miles = fido.calculateWalkLength(90);</code></pre>"],"answer":"01JCGZJVKCH266TSDBW07N343Y"}]