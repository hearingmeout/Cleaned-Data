[{"id":"EZX000003","type":"mcq","stimulusHtml":"<p class=\"dap_body\">Suppose the Boolean expression  <code class=\"dap_inline\">j &lt; numList.size()</code>  in line 4 of the method is replaced with  <code class=\"dap_inline\">j &lt;= numList.size() - 1</code>.  Which of the following best explains the effect of this change?</p>","features":[{"type":"sharedpassage","html":"<div class=\"stimulus_reference\"><p class=\"dap_body\">Consider the following method  <code class=\"dap_inline\">countNegatives</code>,  which returns the number of elements in  <code class=\"dap_inline\">numList</code>  that are less than zero. </p><p class=\"dap_body\"> </p><pre><code class=\"language-plaintext\">public static int countNegatives(ArrayList&lt;Integer&gt; numList)\n{\n   int count = 0;\n   for (int j = 0; j &lt; numList.size(); j++)       // Line 4\n   {\n      if (numList.get(j) &lt; 0)\n      {\n         count++;\n      }\n   }\n   return count;\n}</code></pre></div>"}],"choicesHtml":["<p class=\"dap_body\">It has no effect on the behavior of the method.</p>","<p class=\"dap_body\">It causes the method to ignore the last element in <code class=\"dap_inline\">numList</code>.</p>","<p class=\"dap_body\">It causes an <code class=\"dap_inline\">IndexOutOfBoundsException</code>  to be thrown.</p>","<p class=\"dap_body\">It changes the number of times the loop executes, but all elements in <code class=\"dap_inline\">numList</code> will still be accessed.</p>"],"answer":"01JXVHD0HH1XSY8VRZPT6378HT"}]