[{"id":"VDO000005","type":"mcq","stimulusHtml":"<p class=\"dap_body\">Which of the following changes can be made so that this statement works as intended?</p>","features":[{"type":"sharedpassage","html":"<div class=\"stimulus_reference\"><p class=\"dap_body\">Consider the following statement, which is intended to generate an integer between  <span class=\"dap_inline\">1</span>  and  <span class=\"dap_inline\">10</span>,  inclusive, and assign it to  <span class=\"dap_inline\">result</span>.  This statement does not work as intended.</p><p class=\"dap_body\"> </p><pre><code class=\"language-plaintext\">int result = (int) Math.random() * 10 + 1;</code></pre></div>"}],"choicesHtml":["<p class=\"dap_body\">Add parentheses around the expression <span class=\"dap_inline\">10 + 1</span>, as shown in the following statement. </p><pre><code class=\"language-plaintext\">int result = (int) Math.random() * (10 + 1);</code></pre>","<p class=\"dap_body\">Add parentheses around the expression <span class=\"dap_inline\">Math.random() * 10</span>, as shown in the following statement.</p><pre><code class=\"language-plaintext\">int result = (int) (Math.random() * 10) + 1;</code></pre>","<p class=\"dap_body\">Change the value of <span class=\"dap_inline\">10</span> to <span class=\"dap_inline\">9</span>, as shown in the following statement.</p><pre><code class=\"language-plaintext\">int result = (int) Math.random() * 9 + 1;</code></pre>","<p class=\"dap_body\">Remove the cast of <span class=\"dap_inline\">Math.random()</span> to an <span class=\"dap_inline\">int</span> value, as shown in the following statement.</p><pre><code class=\"language-plaintext\">int result = Math.random() * 10 + 1;</code></pre>"],"answer":"01JCGZA8HJ00FR57MD7T657R4K"}]